---
title: 글 예시
date: 2023-12-18T00:00:00.000Z
description: 블로그 포스트 마크다운 글 작성 예시
toc: true
tags:
  - markdown
  - mdx
---
 
## h2 제목

### h3 제목

---

## 기본 요소

목록
 
- 내용 1
  - 하위 내용 1
- 내용 2

순서 목록

1. 내용 1
2. 내용 2

인용

> 하루하루는 성실하게<br /> 인생 전체는 되는대로

내부 링크

[Home](/)

외부 링크

[My Github Link](https://github.com/kyh196201)

---

## 코드 블록

인라인 코드 블록

`function test() {}` `const str = 'sam'`

기본 코드 블록

```ts {1-2}
import clsx, { type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'
 
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

제목이 있는 코드 블록

````markdown title="utils/index.ts"
```ts title="utils/index.ts"
import clsx, { type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'
 
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
````

줄 번호가 있는 코드 블록

````markdown showLineNumbers
```css showLineNumbers
.prose pre code[data-line-numbers] > [data-line]::before {
  counter-increment: line;
  content: counter(line);
  display: inline-block;
  width: 1rem;
  margin-right: 2rem;
  text-align: right;
  color: var(--code-block-line-number-color);
}
```
````

---

## 테이블

| 제목 | 설명 |
| --- | --- |
| HTML | 웹 페이지의 구조를 정의하는 마크업 언어. |
| CSS | 웹 페이지의 스타일과 레이아웃을 정의하는 언어. |
| Javascript | 웹 개발을 위한 다목적 프로그래밍 언어. |

---

## 이미지

![](/posts/sample/javascript-unsplash.jpg)*이미지 캡션*